{
  "name": "@process-engine/process_engine_meta",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "meta exec \"npm run build\" --exclude process_engine_meta,tslint-config,identity_server && cd _integration_tests && npm run build && cd ..",
    "postinstall": "minstall --no-hoist aurelia-* --trust-local-modules .",
    "reinstall": "./reinstall.sh"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "meta": "^2.2.6",
    "meta-npm": "^1.2.3",
    "minstall": "^3.3.0"
  }
}
